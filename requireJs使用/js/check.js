// JavaScript Documentdefine(function(){	function check(ele1){		$("#winPopLogin .bbs_reg_dl input").blur(function(){			var parents=$(this).parent();			parents.find(".formtips").remove();			//验证用户名			if($(this).is("#username")){				if(this.value=="" || this.value.length<6){					var errorMsg="请至少输入6位的用户名";					parents.append("<div class='formtips onerror'>"+errorMsg+"</div>");					}else{						var okMsg="输入正确";						parents.append("<div class='formtips onsuccess'>"+okMsg+"</div>");						}				}			});				$("#winPopReg .bbs_reg_dl input").blur(function(){			var parents=$(this).parent();			parents.find(".formtips").remove();			if($(this).is("#email")){				if(this.value=="" || (this.value!="" && !/.+@.+\.[a-zA-Z]{2,4}$/.test(this.value))){					var errorMsg="请输入正确的邮箱格式";					parents.append("<div class='formtips onerror'>"+errorMsg+"</div>");					}else{						var okMsg="输入正确";						parents.append("<div class='formtips onsuccess'>"+okMsg+"</div>");						}				}			if($(this).is("#pwd")){				if(this.value=="" || this.value.length<6){					var errorMsg="请输入至少6位的密码";					parents.append("<div class='formtips onerror'>"+errorMsg+"</div>");					}else{						var okMsg="输入正确";						parents.append("<div class='formtips onsuccess'>"+okMsg+"</div>");						}				}			});		}		return{		check:check		}	})